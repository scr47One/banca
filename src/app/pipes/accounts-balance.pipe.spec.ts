import { AccountsBalancePipe } from './accounts-balance.pipe';

describe('AccountsBalancePipe', () => {
  it('create an instance', () => {
    const pipe = new AccountsBalancePipe();
    expect(pipe).toBeTruthy();
  });
});
