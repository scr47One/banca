import { RateDescriptionPipe } from './rate-description.pipe';

describe('RateDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new RateDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
