import { AccountDescriptorPipe } from './account-descriptor.pipe';

describe('AccountDescriptorPipe', () => {
  it('create an instance', () => {
    const pipe = new AccountDescriptorPipe();
    expect(pipe).toBeTruthy();
  });
});
