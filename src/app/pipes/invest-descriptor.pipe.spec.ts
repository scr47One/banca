import { InvestDescriptorPipe } from './invest-descriptor.pipe';

describe('InvestDescriptorPipe', () => {
  it('create an instance', () => {
    const pipe = new InvestDescriptorPipe();
    expect(pipe).toBeTruthy();
  });
});
