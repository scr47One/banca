import { TransactionsListPipe } from './transactions-list.pipe';

describe('TransactionsListPipe', () => {
  it('create an instance', () => {
    const pipe = new TransactionsListPipe();
    expect(pipe).toBeTruthy();
  });
});
