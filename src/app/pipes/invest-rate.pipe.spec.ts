import { InvestRatePipe } from './invest-rate.pipe';

describe('InvestRatePipe', () => {
  it('create an instance', () => {
    const pipe = new InvestRatePipe();
    expect(pipe).toBeTruthy();
  });
});
