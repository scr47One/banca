import { InvestmentsBalancePipe } from './investments-balance.pipe';

describe('InvestmentsBalancePipe', () => {
  it('create an instance', () => {
    const pipe = new InvestmentsBalancePipe();
    expect(pipe).toBeTruthy();
  });
});
